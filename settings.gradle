pluginManagement {
    repositories {
        maven { url 'https://repo.legacyfabric.net/repository/legacyfabric/' }
        maven { url 'https://maven.fabricmc.net/' }
        gradlePluginPortal()
    }
}

rootProject.name = 'legacy-perf'
